#!/usr/local/bin/ruby
# -*- coding: utf-8 -*-

# require "kagura framework file"
pwd = File.expand_path(File.dirname(__FILE__))
pnakotic_lib = File.join([pwd, "kagura.rb"])
Kernel.require(pnakotic_lib)

# execute
run
